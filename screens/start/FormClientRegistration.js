import { View, Text, StyleSheet } from "react-native"

function FormClientRegistration() {
    return (
        <View style={styles.container}>
            <Text>FormClientRegistration</Text>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff',
        alignItems: 'center',
        justifyContent: 'center',
    }
})

export default FormClientRegistration